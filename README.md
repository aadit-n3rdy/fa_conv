# fa_conv
A simple python project to convert NFA to DFA

## Input:
- Accepts an NFA as input in the form of a JSON file

## Output:
- Stores the equivalent DFA in a JSON file

## Visualization:
- Visualize both the NFA and equivalent DFA, during conversion 
